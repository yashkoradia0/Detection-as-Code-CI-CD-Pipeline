name: ssh-bruteforce
displayName: SSH Brute Force Detection
description: Detects 3 or more failed SSH login attempts within 5 minutes.
type: sql
file: ssh_brute_force.sql
categories:
  - authentication
schedule: "*/5 * * * *"
severity: high
riskScore: 75
# Ensure this matches the channel name you created in Step 1
notificationNames:
  - security-alerts
